# 작업 계획

- [x] 현재 UI CSS와 조건 검색 함수/폼 구조를 확인한다
- [x] 전반 UI 높이를 더 조밀하게 줄이고 최소/평균 조건 선택 UI와 필터 로직을 구현한다
- [x] 정적 검증 후 변경 점검, 커밋, 푸시를 수행한다

# 리뷰

- 폼, 탭, 입력창, 버튼, DB 현황 스트립의 padding과 간격을 더 줄여 전체 UI 밀도를 높임
- 조건 검색에서 최소/평균 영업이익률 조건을 각각 켜고 끌 수 있도록 체크박스와 입력값을 한 줄로 배치함
- 스크리닝 함수가 선택된 영업이익률 조건만 필터에 적용하도록 수정함
- `python3 -B -m py_compile app.py`로 문법 검증 완료
- `pytest -q`는 로컬 환경에 `pytest` 명령이 없어 실행하지 못함
- `streamlit` 실행 파일이 없어 실제 화면 렌더링 검증은 수행하지 못함
