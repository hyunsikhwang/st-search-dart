# 작업 계획

- [x] 현재 조건 검색 UI와 `screen_companies_by_margin` 계산 흐름을 확인한다
- [x] 평균 영업이익률 입력값과 필터 조건을 추가한다
- [x] 정적 검증 후 변경 점검, 커밋, 푸시를 수행한다

# 리뷰

- 조건 검색 UI에 `평균 영업이익률 (%)` 입력을 추가함
- 최근 N개 분기의 최소 영업이익률과 평균 영업이익률을 모두 만족하는 회사만 추출하도록 필터를 확장함
- `python3 -B -m py_compile app.py`로 문법 검증 완료
- `pytest -q`는 로컬 환경에 `pytest` 명령이 없어 실행하지 못함
