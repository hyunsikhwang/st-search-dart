# 작업 계획

- [x] 현재 DB 현황 스트립 CSS와 모바일 규칙을 확인한다
- [x] 모바일에서 기준연월별 저장 분포를 낮은 높이로 유지하도록 CSS를 조정한다
- [x] 정적 검증 후 변경 점검, 커밋, 푸시를 수행한다

# 리뷰

- 모바일에서 기준연월 pill 목록이 여러 줄로 감겨 카드 높이가 커지는 문제를 막기 위해 가로 스크롤 단일 행으로 변경함
- 모바일 DB 현황 카드 padding과 label 간격을 줄여 전체 높이를 낮춤
- `python3 -B -m py_compile app.py`로 문법 검증 완료
- `pytest -q`는 로컬 환경에 `pytest` 명령이 없어 실행하지 못함
