# 작업 계획

- [x] 현재 DB 현황 스트립과 검색 탭 위치를 확인한다
- [x] DB 저장 현황을 워크스페이스 탭 위로 이동한다
- [x] 정적 검증 후 변경 점검, 커밋, 푸시를 수행한다

# 리뷰

- DB 저장 현황과 기준연월별 저장 분포 스트립을 워크스페이스 검색 UI 위로 이동함
- `python3 -B -m py_compile app.py`로 문법 검증 완료
- `pytest -q`는 로컬 환경에 `pytest` 명령이 없어 실행하지 못함
